export interface SnackbarState {
    show: boolean,
    multiLine: boolean,
    timeout: number,
    bottom: boolean,
    top: boolean,
    vertical: boolean,
    color: string,
    message: string,
}